<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:local="clr-namespace:AhkControls;assembly=WPF_Runner"
        Background="#0F1A2A" WindowState="Normal"
        WindowStyle="None" AllowsTransparency="True">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="8,8,0,0">
      <Button Name="BtnClose" Content="X" Width="30" Height="26"
              Background="#C0392B" Foreground="White" FontWeight="Bold"
              BorderThickness="0" DockPanel.Dock="Right" Margin="0,0,8,0"/>
      <TextBlock Text="Dynamic Form Builder" FontSize="18"
                 FontWeight="Bold" Foreground="#DCEFFF" VerticalAlignment="Center"/>
    </DockPanel>

    <StackPanel Grid.Row="1" Margin="8,0,8,4">
      <TextBlock Text="Form definition (type|name|label|default|options|validation):"
                 Foreground="#8AB4CC" FontSize="11" Margin="0,0,0,2"/>
      <TextBox Name="DefEditor" Height="140" FontSize="11" AcceptsReturn="True"
               TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
               Background="#1A2A3A" Foreground="#DCEFFF" BorderBrush="#2A4A6A">
        <TextBox.Text>text|Name|Full name:|||required
text|Email|Email:|||required|regex:.+@.+\..+
combo|Country|Country:||Argentina,Mexico,Spain,Chile,Peru,Colombia|required
number|Age|Age:|25||1-120
check|Terms|I accept terms:||required
date|StartDate|Start date:|||
textarea|Notes|Additional notes:|||</TextBox.Text>
      </TextBox>
      <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
        <Button Name="BtnBuild" Content="Build Form" Width="110" Height="28" Margin="0,0,8,0"/>
        <Button Name="BtnResetForm" Content="Reset" Width="80" Height="28"/>
      </StackPanel>
    </StackPanel>

    <local:FormBuilderControl Grid.Row="2" Name="FormView" Margin="8,0,8,8"/>
  </Grid>
</Window>
